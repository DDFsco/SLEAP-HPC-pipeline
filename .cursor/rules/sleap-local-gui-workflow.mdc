---
description: SLEAP Local GUI Workflow implementation constraints
globs:
  - "gl_sync/**"
alwaysApply: true
---

Use `/Users/ddfsco/Projects/Great-Lakes` as the active workspace for the SLEAP Local GUI Workflow. The old Desktop path can hang during filesystem access.

Follow `CURSOR_HANDOFF.md` in the repository root.

Primary implementation target:

- `gl_sync/install_local_gui.sh`
- `gl_sync/install_local_gui.ps1`
- `gl_sync/pipeline_lib.py`
- `gl_sync/sleap_pipeline_gui.py`
- `gl_sync/README_LOCAL_GUI.md`

Do not rewrite existing Great Lakes scripts unless necessary:

- `install.sh`
- `train.sh`
- `predict.sh`
- `sleap_common.sh`

Keep training and prediction on Great Lakes. The local GUI environment is for labeling, task orchestration, and exporting training packages.
